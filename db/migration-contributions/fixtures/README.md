# Migration contract fixtures

Files here exist only to test contribution discovery and rejection behavior. The central Flyway assembler must never read this directory as production migration input, and fixtures must not use the `.sql` extension.
