# Canonical trading migrations

Production contribution SQL is placed directly in this directory after canonical DBML review. No migration is present yet because the F01-F14 shadow schema has not been reconciled with the canonical model.

Only files matching the `filename.regex` in `../contribution.properties` are eligible for the central bundle. This explanatory file keeps the empty boundary visible without pretending to be a migration.
