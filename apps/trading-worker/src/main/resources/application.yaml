spring:
  application:
    name: trading-worker
  main:
    web-application-type: none
    banner-mode: "off"
    keep-alive: true
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: false

trading:
  fake-candidate:
    enabled: false
