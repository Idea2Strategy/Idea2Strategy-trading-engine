# The vendored D92 conformance fixture is digest-pinned shared bytes; a CRLF rewrite on
# checkout would make the Java and Python bindings disagree about the same file.
modules/strategy-runtime/src/test/resources/conformance/** -text
