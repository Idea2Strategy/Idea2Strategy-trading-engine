package com.idea2strategy.trading.strategy.runtime.candidate;

public enum CandidateResolutionReason {
    ACCEPTED_AS_SUBMITTED,
    EXACT_DUPLICATE_COLLAPSED,
    CANDIDATE_IDENTITY_CONFLICT,
    OPPOSING_ACTION_CONFLICT
}
